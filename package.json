{
	"name": "lraj23-self-bot",
	"version": "0.1.0",
	"description": "Represents @lraj23 in various occasions for various reasons.",
	"main": "app.js",
	"type": "module",
	"scripts": {
		"start": "node --env-file=.env app.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"license": "MIT",
	"dependencies": {
		"@slack/bolt": "^4.2.0"
	}
}